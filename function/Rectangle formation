#include<iostream>

using namespace std;

int main (){
  int a,b,c,d;
  cout<<"Enter 4 sides of rectangle : "<<endl;
  cin>>a>>b>>c>>d;

  if((a==b && c == d) || (a == d && c == b) || (a == c && d == b)){
             cout<<"Rectangle is possible";

  }
  
  else {
    cout<<"Rectangle is not possible";
  }


  return 0;
}
